# 本地提示词管理器使用指南

## 启动

双击 `启动数据库.bat`。它会同步基础 JSON 数据并打开本地管理页面，不需要单独运行 JSON，也不需要安装数据库软件。

## 后续导入提示词

1. 点击右上角“导入 / 新增”。
2. 拖入一个或多个 `.txt`、`.md`、`.json` 文件。
3. 建议保持“自动识别分类……”和“跳过重复记录”勾选。
4. 导入完成后，在左侧找到新记录。
5. 点击详情页的“编辑与分类”，可修改分类、合集、标签和正文。

TXT 和 Markdown 默认每个文件生成一条记录。JSON 支持：

- 管理器导出的完整数据库备份；
- 提示词对象数组；
- 单条提示词对象。

自动分析会尝试识别分类、画幅、时长、分辨率、标签，以及 `[00:00-00:03]` 或 `[0-3 秒]` 格式的分镜。

## 手动新增

点击“导入 / 新增”，切换到“手动新增”，填写标题和完整提示词。点击“自动分析”可自动补充分组信息，再点击“保存提示词”。

## 分类与整理

- 分类：内容类型，例如“搞笑短视频”“奇幻动作”。
- 合集：你自己的项目或系列，例如“水豚噜噜系列”“待测试”。
- 标签：可同时拥有多个关键词，用逗号分隔。
- 左侧筛选器可以组合使用分类、画幅和合集。

## 保存和备份

新增、编辑、分类和删除会自动保存在当前浏览器的本地数据库中，不会覆盖原始 TXT、Markdown、SQLite 或基础 JSON 文件。

请尽量一直使用同一个默认浏览器。更换电脑、换浏览器或清除浏览器数据前，点击右上角“导出备份”：

1. 页面会先显示备份记录数、分镜数和完整 JSON 预览。
2. 点击“下载 JSON 文件”保存备份。
3. 如果浏览器拦截下载，可点击“复制 JSON”，或在预览框中按 `Ctrl+A`、`Ctrl+C` 后粘贴到新的 `.json` 文件。
4. 恢复时把备份 JSON 重新拖入导入窗口即可。

建议每次大量整理后导出一次备份。

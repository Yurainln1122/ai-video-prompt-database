# 本地提示词管理器使用指南

## 启动

双击 `启动数据库.bat`。它会同步基础 JSON 数据并打开本地管理页面，不需要单独运行 JSON，也不需要安装数据库软件。

## 导入视频提示词

1. 点击右上角“🎬 视频”分组中的“＋ 导入”。
2. 拖入一个或多个 `.txt`、`.md`、`.json` 文件。
3. 建议保持“自动识别分类……”和“跳过重复记录”勾选。
4. 导入完成后，在左侧找到新记录。
5. 点击详情页的“编辑与分类”，可修改分类、合集、标签和正文。

TXT 和 Markdown 默认每个文件生成一条记录。JSON 支持：

- 管理器导出的完整数据库备份；
- 提示词对象数组；
- 单条提示词对象。

自动分析会尝试识别分类、画幅、时长、分辨率、标签，以及 `[00:00-00:03]` 或 `[0-3 秒]` 格式的分镜。

## 导入图片提示词

1. 点击右上角“▧ 图片”分组中的“＋ 导入”。
2. 拖入一个或多个 `.txt`、`.md`、`.json` 文件。
3. 系统会按图片规则识别人物肖像、产品摄影、概念场景、动漫插画、海报设计、摄影写实或 3D / CG 等分类。
4. 导入后可编辑标题、画幅、分类、合集、标签、完整提示词和负面提示词。
5. 左侧点击“图片”，可只查看图片提示词。

图片提示词不会生成时长和分镜，导出时也不会混入视频备份。

## 手动新增

从视频或图片的“＋ 导入”入口进入后，切换到“手动新增”，填写标题和完整提示词。也可以在表单中切换“提示词类型”。点击“自动分析”可自动补充分组信息，再点击“保存提示词”。

## 分类与整理

- 分类：内容类型，例如“搞笑短视频”“奇幻动作”。
- 合集：你自己的项目或系列，例如“水豚噜噜系列”“待测试”。
- 标签：可同时拥有多个关键词，用逗号分隔。
- 左侧“全部 / 视频 / 图片”可切换媒介，随后组合使用分类、画幅和合集筛选。

### 建立和管理合集

1. 在左侧“合集”标题右侧点击“＋ 新建 / 管理”。
2. 输入合集名称并点击“＋ 建立合集”；空合集也会立即保存。
3. 在合集管理窗口可以重命名或删除合集。
4. 删除合集不会删除提示词，其中的提示词会自动移入“未归档”。
5. 编辑或新增提示词时，也可以点击合集字段右侧的“＋ 新建合集”，建立后会自动填入当前表单。

## 保存和备份

新增、编辑、分类和删除会自动保存在当前浏览器的本地数据库中，不会覆盖原始 TXT、Markdown、SQLite 或基础 JSON 文件。

请尽量一直使用同一个默认浏览器。更换电脑、换浏览器或清除浏览器数据前，分别导出两类备份：

1. 点击“🎬 视频 → ⇩ 导出”，生成只包含视频提示词及分镜的 JSON。
2. 点击“▧ 图片 → ⇩ 导出”，生成只包含图片提示词的 JSON。
3. 页面会先显示记录数和完整 JSON 预览。
4. 点击“下载 JSON 文件”保存备份。
5. 如果浏览器拦截下载，可点击“复制 JSON”，或在预览框中按 `Ctrl+A`、`Ctrl+C` 后粘贴到新的 `.json` 文件。
6. 恢复时从同类型的导入入口重新拖入备份 JSON。

建议每次大量整理后导出一次备份。
